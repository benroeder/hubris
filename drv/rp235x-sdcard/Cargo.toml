[package]
name = "drv-rp235x-sdcard"
version = "0.1.0"
edition = "2021"

[dependencies]
cortex-m = { workspace = true }
idol-runtime = { workspace = true }
num-traits = { workspace = true }
zerocopy = { workspace = true }
zerocopy-derive = { workspace = true }
rp235x-pac = { workspace = true }

drv-rp235x-sdcard-api = { path = "../rp235x-sdcard-api" }
drv-rp235x-sys-api = { path = "../rp235x-sys-api" }
counters = { path = "../../lib/counters" }
userlib = { path = "../../sys/userlib" }

[features]
# SB Components MusicPi HAT: TF slot on SPI0, SCK=GP18 MOSI=GP19 MISO=GP16
# CS=GP17 (manual SIO). Default: SPI1 on GP10-13 (the SPI1 breakout wiring).
musicpi = []

[build-dependencies]
idol = { workspace = true }

[[bin]]
name = "drv-rp235x-sdcard"
test = false
doctest = false
bench = false

[lints]
workspace = true
