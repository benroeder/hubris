gdb_port 3333
telnet_port 4444

# OpenOCD configuration for the RP235x.
# NB: requires the Raspberry Pi OpenOCD fork for `target/rp2350.cfg` (upstream
# 0.12 ships only rp2040). Prefer `probe-rs` 0.31, which supports RP2350 natively.
source [find interface/cmsis-dap.cfg]
source [find target/rp2350.cfg]
